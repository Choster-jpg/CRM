const express = require('express');
const router = express.Router();

router.post('/register',);
router.post('/login',);
router.post('/logout',);
router.post('/reset',);

router.get('/activate',);
router.get('/reset',);
router.get('/auth',);
